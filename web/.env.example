# Railway 後端網址（結尾不要斜線）
VITE_API_BASE=https://你的專案.up.railway.app

# 登入用。這兩個是「公開」金鑰，放在前端是正常且安全的設計：
# 它只能用來登入，碰不到任何資料（資料表都開了 RLS 且沒有政策）。
# 值取自 Supabase → Project Settings → API Keys 的 publishable / anon。
VITE_SUPABASE_URL=https://你的專案.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
